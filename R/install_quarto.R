# Run once in R or RStudio to install the Quarto R package.
# Then you can render QMD files with: quarto::quarto_render("data_cleaning.qmd")

install.packages("quarto", repos = "https://cloud.r-project.org")
